<%= form_with(model:[anime, post_comment]) do |f| %>
  <%= f.text_area :comment, rows:'1',placeholder: "コメントをここに", class:"form-control" %></br>
  <%= f.submit "送信する", class: "btn btn-success float-right" %></br>
<% end %>